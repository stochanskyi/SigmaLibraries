name: test-and-build

on:
  pull_request:
    branches:
      - 'dev'

jobs:
  Unit-tests:
    name: Unit tests running
    runs-on: ubuntu-latest
    environment: prod
    steps:

      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: Run Unit tests
        run: ./gradlew test --stacktraces